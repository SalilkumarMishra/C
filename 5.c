#include<stdio.h>
int main(){
    float a=3.14;
    float b=3.24;
    printf("Sum is %.2f",a+b);
}
// we can take %.2f to get 2 decimal place we can change to 3 or 4 but bydefault it will give 6 decimal.